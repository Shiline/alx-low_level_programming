#include "main.h"

/**
 * main - prints the numbers 0 to 9 followed by a new line
 *
 * Return: 0 always
 */

void print_numbers(void)
{
	for (i = 0 ; i <= 9 ; i++)
		_putcha("%d/n", i);
	return (0);
}
